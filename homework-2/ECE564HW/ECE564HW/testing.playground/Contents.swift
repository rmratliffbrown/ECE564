import UIKit
import Foundation

//func fromBase64(image: String)  -> UIImageView{
    //   let base64Decoded = Data(base64Encoded: image)!
    //let decodedString = String(data: base64Decoded, encoding: .utf8)!
   
    
    //return 0//decodedString
//}

// file to String

//let fileURL = URL (fileURLWithPath: )

//let fileData = try Data (contentsOf: fileURL)

//let imageString = fileData.base64EncodedString ()

// string to image

//let imageData = Data (base64Encoded: imageString)!

//let image = NSImage (data: imageData)!

//let data = image?.jpegData(compressionQuality: 0.9)

// convert image to png
// var imageData = UIImagePNGRepresentation(image)

// convert image to jpeg
// var imageData = UIImageJPEGRepresentation(image, 0.7)
